{
  "name": "@knowledge-base/web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "next dev --port ${WEB_PORT:-3002}",
    "build": "next build",
    "start": "next start --port ${WEB_PORT:-3002}",
    "lint": "eslint .",
    "check-types": "next typegen && tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@knowledge-base/contracts": "workspace:*",
    "@knowledge-base/ui": "workspace:*",
    "lucide-react": "catalog:",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@knowledge-base/eslint-config": "workspace:*",
    "@knowledge-base/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "eslint": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
